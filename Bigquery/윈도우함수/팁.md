#### 0 
```
order by와 partition by를 쓸 지/ 무엇을 쓸 지 잘 고민해야한다
```
- order by 필수 = 탐색함수, 번호지정 함수
- order by 보통 필요 x = 집계함수(단, frame을 설정할떈 필요할 수 있다)
- order by 이후 frame 안 쓰면 기본값은 unbounded preceding and current row

#### 탐색함수 정의 
> 파티션 범위에서 order by 기준 이전/이후/첫/마지막 값

#### 번호지정 함수 정의
> 파티션 범위에서 order by 기준 순위 / 순서


#### 1
```
윈도우 함수에서 PARTITION은 '비교/순서를 매길 모집단'
```
#### 2
```
ORDER BY를 “정렬 컬럼”이 아니라 '아무거나'로 쓰는 습관을 고처야함
```

```
“이전/첫/누적/롤링” 들어가면 ORDER BY는 무조건 시간/순서 컬럼

비율 분모가 전체합이면 윈도우 SUM에 ORDER BY 넣지 말기
```

#### 3
> group by는 행을 줄이고   
> 윈도우 함수는 열을 추가하며 행을 줄이지 않는다

> 반드시 고려해야하는 사항임(잘못하면 중복 발생)